"""
LangGraph graphs: `copilot` (the ReAct copilot) and `responder` (the away-assistant
two-stage airlock — see `responder.py`, `polaris_agent/state.py`, and the pure gates
in `polaris_agent/disclosure.py`).
"""

from __future__ import annotations

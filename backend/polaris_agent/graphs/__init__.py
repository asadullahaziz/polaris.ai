"""
LangGraph graphs (architecture §4–5). Graph 1 = copilot (P2); Graph 2 = the away-assistant
responder (P4). Graph 2 is the redesigned, principal-centric airlock (stance-not-role,
triage + conditional assess) — see `responder.py`, `polaris_agent/state.py`, and the
pure gates in `polaris_agent/disclosure.py`.
"""

from __future__ import annotations

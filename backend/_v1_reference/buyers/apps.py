from django.apps import AppConfig


class BuyersConfig(AppConfig):
    """Buyers & prospects: prospect, buy_box, buy_box_geo, purchase (P1)."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "buyers"
